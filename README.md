# codex-usage (OpenClaw skill)

OpenClaw skill wrapper for the local `codex-usage` CLI.

## Purpose
Returns concise Codex usage information by running:

```bash
codex-usage --plain
```

## Prerequisites
- `codex-usage` installed on PATH on the OpenClaw host.

## License
GPL-3.0
